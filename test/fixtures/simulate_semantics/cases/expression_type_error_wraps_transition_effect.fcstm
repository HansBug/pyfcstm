def int x = 0;
def float y = 1.5;

state Root {
    state A {
        during { x = x + 1; }
    }
    state B {
        during { x = x + 10; }
    }

    [*] -> A;
    A -> B :: Go effect {
        tmp = y + 0.5;
        x = x + 100;
        y = tmp << 1;
    };
}
