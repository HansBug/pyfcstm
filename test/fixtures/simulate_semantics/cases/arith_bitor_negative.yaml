title: "Bitwise or with a negative operand"
origin:
  files:
  - https://github.com/HansBug/pyfcstm/issues/449
  notes:
  - "Fills zero corpus coverage for bitwise or with a negative literal operand. The operands share a set bit, so the result differs from bitwise xor of the same pair."
categories:
- runtime
- template_alignment
steps:
- cycle: []
  expect:
    state: Root.A
    vars_exact:
      r: -5
    ended: false
- cycle: []
  expect:
    state: Root.A
    vars_exact:
      r: -5
    ended: false
