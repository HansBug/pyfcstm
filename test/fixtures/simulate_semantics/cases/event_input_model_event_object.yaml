title: event path input reaches the same transition as the prior model-event case
origin:
  files:
  - test/simulate/test_semantic_fixtures.py::test_simulation_semantic_fixture
  - test/template/python/test_semantic_fixture_alignment.py::test_generated_python_alignment_semantic_fixture
  docs:
  - https://github.com/HansBug/pyfcstm/issues/143#issuecomment-4614288612
  notes:
  - Legacy model Event descriptor coverage is now represented by a legal string event path in the shared string-event contract; object-input API coverage stays in ordinary simulator tests.
categories:
- event_paths
- runtime
- template_alignment
steps:
- cycle: []
  expect:
    state: Root.A
    vars:
      x: 1
    ended: false
- cycle: Root.A.Go
  expect:
    state: Root.B
    vars:
      x: 11
    ended: false
