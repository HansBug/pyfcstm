// Demo: E_IMPORT_ALIAS_CONFLICT — import alias clashes with a sibling
// regular state. The alias `Idle` collides with `state Idle;` above.
// Expected:
//   * E_IMPORT_ALIAS_CONFLICT on `as Idle` (line 5)

state Root {
    state Idle;
    [*] -> Idle;
    import "./worker.fcstm" as Idle;
}
