def int base = 2;
def int exponent = 3;
def int r = 0;

state Root {
    state A {
        during {
            r = base ** exponent;
            base = base - 2;
            exponent = exponent - 4;
        }
    }

    [*] -> A;
}
