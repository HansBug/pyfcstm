title: Combo transition trigger exit fallback alignment
origin:
  files:
  - test/simulate/test_combo_runtime_compat.py::test_exit_combo_parent_continuation_failure_rolls_back_to_source
  - test/simulate/test_combo_runtime_compat.py::test_missing_second_event_rolls_back_and_uses_later_fallback_transition
  notes:
  - Covers an exit combo that reaches the parent continuation boundary and is rejected by a false parent guard; validation must roll back child exit and terminal effects before selecting a later same-source fallback transition.
  - The fixture is consumed by both the simulator and generated Python runtime alignment runners.
categories:
- runtime
- template_alignment
- pseudo_chain
- lifecycle
steps:
- cycle: []
  expect:
    state: Root.Parent.S
    vars:
      gate: 0
      value: 0
    ended: false
- cycle:
  - Root.Parent.S.E1
  - Root.Parent.S.E2
  expect:
    state: Root.Parent.Fallback
    vars:
      gate: 0
      value: 121
    ended: false
