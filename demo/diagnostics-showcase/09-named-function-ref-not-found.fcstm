// Demo: E_NAMED_FUNCTION_REF_NOT_FOUND — ``ref`` path doesn't resolve.
// Expected:
//   * E_NAMED_FUNCTION_REF_NOT_FOUND on the ``ref NoSuch.Action`` line.

state Root {
    state Idle {
        enter ref NoSuch.Action;
    }
    state Active;
    [*] -> Idle;
    Idle -> Active :: Go;
}
