title: "Integer modulo with a negative dividend follows floored division"
origin:
  files:
  - https://github.com/HansBug/pyfcstm/issues/449
  notes:
  - "Python/simulator modulo is floored, so -7 % 2 is 1 rather than the C truncated -1."
categories:
- runtime
- template_alignment
steps:
- cycle: []
  expect:
    state: Root.A
    vars_exact:
      r: 1
    ended: false
- cycle: []
  expect:
    state: Root.A
    vars_exact:
      r: 1
    ended: false
