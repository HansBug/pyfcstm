title: Combo transition trigger pseudo aspect alignment
origin:
  files:
  - test/fixtures/simulate_semantics/cases/combo_transition_trigger_pseudo_aspect_alignment.fcstm
  notes:
  - Covers generated combo pseudo states under ancestor during before/after aspect actions.
  - The intermediate generated pseudo state must keep the ordinary pseudo contract and must not execute ancestor aspects; only the stable source and target leaf cycles should execute those aspects.
categories:
- runtime
- template_alignment
- pseudo_chain
- lifecycle
steps:
- cycle: []
  expect:
    state: Root.Start
    vars:
      trace: 101000
    ended: false
- cycle:
  - Root.Start.A
  - Root.Start.B
  expect:
    state: Root.Done
    vars:
      trace: 202011
    ended: false
