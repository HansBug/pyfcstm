// Demo: E_IMPORT_NOT_FOUND — import statement points at a file that
// doesn't exist. Open this file in VSCode; the Problems panel should
// show:
//   * E_IMPORT_NOT_FOUND on `"./absent.fcstm"` (line 4)

state Root {
    state Idle;
    [*] -> Idle;
    import "./absent.fcstm" as Worker;
}
