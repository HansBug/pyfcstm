title: Combo transition trigger unstable predecessor alignment
origin:
  files:
  - test/simulate/test_combo_runtime_compat.py::test_missing_second_event_rolls_back_and_uses_later_fallback_transition
  - test/simulate/test_combo_runtime_compat.py::test_middle_guard_false_rolls_back_and_uses_later_fallback_transition
  - test/verify/test_smt_local.py::TestTransitionShadowedByPredecessor::test_validation_rejected_predecessor_is_not_deterministic_shadow
  notes:
  - Covers a fully matched combo predecessor whose target composite cannot reach a stable child, so runtime validation rejects it and a later fallback transition still wins.
  - The fixture is consumed by both the simulator and generated Python runtime alignment runners; the verify origin remains covered by an ordinary pytest assertion.
categories:
- runtime
- template_alignment
- pseudo_chain
- validation
steps:
- cycle: []
  expect:
    state: Root.S
    vars:
      gate: 0
      value: 0
    ended: false
- cycle:
  - Root.S.E1
  - Root.S.E2
  expect:
    state: Root.Fallback
    vars:
      gate: 0
      value: 101
    ended: false
