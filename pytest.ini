[pytest]
timeout = 300
testpaths =
    test
norecursedirs =
    llm_eval
markers =
    unittest
    benchmark
    ignore
    slow: tests that invoke native toolchains (cmake / cc) and take significantly longer
    native_toolchain: explicit native compiler matrix tests that are skipped unless opted in
