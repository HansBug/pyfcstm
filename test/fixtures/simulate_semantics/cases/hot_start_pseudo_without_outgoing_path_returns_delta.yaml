title: Hot start no-outgoing pseudo returns a Delta step
origin:
  files:
  - test/simulate/test_semantic_fixtures.py::test_simulation_semantic_fixture
  - test/template/python/test_semantic_fixture_alignment.py::test_generated_python_alignment_semantic_fixture
  notes:
  - A pseudo state with no outgoing control edge is classified before its during
    action, so the first real cycle stutters without executing it.
categories:
- runtime
- hot_start
- validation
- pseudo_chain
- template_alignment
initial:
  state: Root.P
  vars:
    x: 0
steps:
- cycle: []
  cycle_count: 2
  expect:
    state: Root.P
    vars:
      x: 0
    ended: false
    delta: true
