# Native MiniRacer distribution selected by the Python support boundary.
# Python 3.7 has no compatible modern mini-racer wheel, so it uses the
# legacy package; Python 3.8+ uses the maintained distribution without a
# brittle upper pin.
py-mini-racer>=0.6.0,<0.7; python_version < "3.8"
mini-racer>=0.7.0; python_version >= "3.8"
