def int trace = 0;

state Root {
    >> during before { trace = trace + 1000; }
    >> during after { trace = trace + 100000; }

    state Start;

    state Done {
        enter { trace = trace + 10; }
    }

    [*] -> Start;
    Start -> Done :: A + B effect { trace = trace + 1; }
}
