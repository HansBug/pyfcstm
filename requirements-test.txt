coverage>=5
mock>=4.0.3
flake8>=3.5
testfixtures>=6.18.5
pytest>=6.2.5
pytest-cov>=3.0.0
pytest-mock>=3.6.1
pytest-xdist>=1.34.0
pytest-rerunfailures>=10.2
pytest-timeout>=2.0.2
pytest-benchmark>=3.4.0
easydict>=1.7,<2
testtools>=2
where>=1.0.2
natsort
cmake<4
# ruff 0.16 turned a large set of pyupgrade/refactor rules on by default.
# Those defaults conflict with the Python 3.7 floor this project supports,
# so the lint contract stays on the 0.15 rule set until it is retargeted.
ruff<0.16
# Draft 2020-12 validation backs the frozen "schema and constructor accept the
# same payload set" contract.  jsonschema 4 requires Python 3.8, and 3.2 has no
# Draft202012Validator, so the 3.7 cell keeps skipping those checks.
jsonschema>=4; python_version>='3.8'
