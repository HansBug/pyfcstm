[pytest]
timeout = 300
testpaths =
    test
norecursedirs =
    llm_eval
markers =
    unittest
    benchmark
    ignore
    slow: tests that invoke native toolchains (cmake / cc) and take significantly longer
