title: Combo transition trigger duplicate event alignment
origin:
  files:
  - test/diagnostics/test_combo_inspect_contract.py::test_combo_duplicate_event_warning_points_to_second_term_and_first_related_term
  - test/simulate/test_combo_runtime_compat.py
  notes:
  - Exercises duplicate event terms as executable runtime semantics, not only as an inspect warning, so list-like event consumption cannot regress into set-like handling in generated runtime alignment.
categories:
- runtime
- template_alignment
- pseudo_chain
- event_paths
steps:
- cycle: []
  expect:
    state: Root.Start
    vars:
      score: 0
    ended: false
- cycle:
  - Root.Start.Same
  expect:
    state: Root.DuplicateAccepted
    vars:
      score: 101
    ended: false
- cycle:
  - Root.DuplicateAccepted.Finish
  expect:
    state: Root.Done
    vars:
      score: 101
    ended: false
