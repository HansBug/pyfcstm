def int x = 0;
def int y = 0;

state Root {
    state A {
        during {
            if [x != 0 && 10 / x > 1] {
                y = 100;
            }
            y = y + 1;
        }
    }

    [*] -> A;
}
